<div id="breadcrumbs">
	<a href="/">WavHead</a> > <%= @artist.name %>
</div>

<div class="artlist">
  <% @artist.albums.each do |a| %>
    <div class="artlistitem">
  <a href="/browse/<%= @artist.name %>/<%= a.title %>"><%= a.title %></a>
</div>
<% end %>
</div>
